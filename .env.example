# Environment variables for InfraMon

# Backend settings
SECRET_KEY=your-super-secret-key-change-in-production-min-32-chars
DEBUG=true
ADMIN_USERNAME=admin
ADMIN_PASSWORD=generate-a-strong-random-password-here

# Database settings
DATABASE_URL=sqlite+aiosqlite:///./inframon.db

# Frontend settings (optional, defaults provided)
VITE_API_URL=http://localhost:8065/api/v1

# Docker settings
DOCKER_SOCKET_PATH=/var/run/docker.sock
DOCKER_API_VERSION=1.45

# Podman settings (for Podman systems)
PODMAN_SOCKET_PATH=/run/user/0/podman/podman.sock

# Colima settings (for Colima on macOS/Linux)
COLIMA_SOCKET_PATH=/run/colima/socket/docker.sock

# Container runtime selection
# Options: "auto", "docker", "podman", "colima"
# "auto" will auto-detect the available runtime
CONTAINER_RUNTIME=auto
